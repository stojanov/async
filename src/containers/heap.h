#include <pch.h>

namespace async::cont {

template <typename T, typename SortFunc> class heap {
  public:
    void insert();

    void remove();

    // binary
    void search();
};
} // namespace async::cont
